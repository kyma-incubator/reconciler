apiVersion: v1
kind: Service
metadata:
  name: {{ include "testmeplz.fullname" . }}
  labels:
    {{- include "testmeplz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testmeplz.selectorLabels" . | nindent 4 }}
