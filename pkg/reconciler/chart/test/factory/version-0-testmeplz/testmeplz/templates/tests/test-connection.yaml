apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testmeplz.fullname" . }}-test-connection"
  labels:
    {{- include "testmeplz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testmeplz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
