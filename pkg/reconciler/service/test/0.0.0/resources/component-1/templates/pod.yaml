apiVersion: v1
kind: Pod
metadata:
  name: dummy-pod
  namespace: default
spec:
  containers:
    - name: dummy-pod
      image: debian
      command: ["/bin/sh"]
      args: ["-c", "sleep 1 && echo Sleep expired > /dev/termination-log"]
