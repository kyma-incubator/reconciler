db:
  driver: sqlite
  encryption:
    keyFile: "./encryption/unittest.key"
  postgres:
    host: "localhost"
    database: "kyma"
    port: 5432
    user: kyma
    password: kyma
    useSsl: false
  sqlite:
    file: "test/unittest.db"
    deploySchema: true
    resetDatabase: true